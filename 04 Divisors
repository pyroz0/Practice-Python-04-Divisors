# Create a program that asks the user for a number and then
# prints out a list of all the divisors of that number
l = []
num = int(input("insert the number to know its divisors: "))
for i in range(1, num):
    if num % (i) == 0:
        l.append(i)
        i = i + 1
    else:
        continue
print(l)

# works great for numbers < 1,000,000,000
